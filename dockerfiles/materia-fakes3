FROM ruby:2.3.4-alpine

RUN apk add --no-cache \
  python \
  python-dev \
  py-pip \
  bash

RUN pip install watchdog

# install fake-s3
RUN gem install fakes3 -v 0.2.4

# run fake-s3
RUN mkdir -p /s3mnt

EXPOSE 10001
